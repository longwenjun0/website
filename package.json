{
  "name": "website_test",
  "private": true,
  "scripts": {
    "start": "concurrently \"wait-on tcp:5000 && npm run dev --prefix client\" \"npm run start --prefix server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "wait-on": "^8.0.4"
  },
  "dependencies": {
    "body-parser": "^2.2.0",
    "express": "^5.1.0",
    "mysql2": "^3.14.5",
    "openai": "^5.12.2"
  }
}
